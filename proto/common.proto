syntax = "proto2";

import "commands.proto";
import "data.proto";

package proto;

message Data {
  oneof payload {
    InfoMessagePacket info_packet = 1;
  }
}

message Command {
  oneof payload {
    TestCommand test_command = 1;
  }
}